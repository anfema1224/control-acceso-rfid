services:
  - type: web
    name: control-acceso-nusefa
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: ADMIN_USER
        value: admin
      - key: ADMIN_PASSWORD
        value: Fatima2026*
      - key: DATA_DIR
        value: /tmp/control-acceso-data
